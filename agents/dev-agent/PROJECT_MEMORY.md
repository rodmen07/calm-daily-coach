# Project Memory - Autonomous Dev Agent

Auto-generated by `memory.py`. This is the cumulative knowledge the agent has
learned while working the backlog. It is injected into future task prompts so the
agent avoids repeating past mistakes. Do not edit by hand - it is regenerated each run.

_Tasks recorded: 0 succeeded, 0 failed._

## Pitfalls to avoid

- Tailwind v4: use bg-(--var) or bg-[var(--var)] for CSS-variable utilities; the bg-[--var] form compiles to invalid CSS and renders nothing. _(seen 1x)_
- Type React elements as React.ReactElement (or import JSX from react); the global JSX.Element namespace is not available under this TS/React config. _(seen 1x)_
- Never use `any`; declare explicit interfaces/types - the lint config rejects `any`. _(seen 1x)_
- Remove unused variables, imports, and parameters; the lint config forbids them. _(seen 1x)_
- Verify import paths against real files and only import packages already listed in package.json. _(seen 1x)_
- Follow the Rules of Hooks: no setState in a render/effect body without a guard, keep hook calls unconditional, and satisfy exhaustive-deps. _(seen 1x)_
- Check exact prop/field names and shapes against the type definitions before using them. _(seen 1x)_
- Add the 'use client' directive at the top of any component that uses hooks, state, or browser APIs. _(seen 1x)_

## Conventions

- React components live under src/app/components/.
- Shared logic and helpers live under src/lib/.
- Route pages are src/app/<route>/page.tsx (Next.js App Router).
- Only import packages already listed in package.json.
